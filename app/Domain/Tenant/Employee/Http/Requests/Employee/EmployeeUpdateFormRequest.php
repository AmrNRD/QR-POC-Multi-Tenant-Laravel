<?php

namespace App\Domain\Tenant\Employee\Http\Requests\Employee;
use App\Domain\Tenant\Employee\Http\Requests\Employee\EmployeeStoreFormRequest;

class EmployeeUpdateFormRequest extends EmployeeStoreFormRequest
{
    /**
     * Determine if the employee is authorized to make this request.
     *
     * @return bool
     */
    public function authorize()
    {
        return true;
    }

    /**
     * Get the validation rules that apply to the request.
     *
     * @return array
     */
    public function rules()
    {
        $rules = [
        // 'email'    => ['required','unique:employees,name,'.$this->route()->parameter('employee').',id'],
        ];

        return array_merge(parent::rules(), $rules);
    }

    /**
     * Get custom attributes for validator errors.
     *
     * @return array
     */
    public function attributes()
    {
        return parent::attributes();
    }
}
